pipeline {
    agent any
    stages {
       stage('Run Tests') {
          steps {
             echo 'test'
          }
       }
       stage('Build') {
          steps {
             echo 'build'
          }
       }
       stage('Deploy to Production') {
           steps {
             echo 'deply'
          }
       }
  }
  }
